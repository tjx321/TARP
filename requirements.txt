# TARP runtime dependencies (tested with Python 3.10).
# For CUDA 11.8 wheels, install torch/torchvision from the PyTorch index, e.g.:
#   pip install torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu118
torch>=2.0
torchvision>=0.15
numpy>=1.23
PyYAML>=6.0
Pillow>=9.0
tqdm>=4.65
scikit-learn>=1.3
scipy>=1.10
ftfy>=6.1
regex>=2023.0
gdown>=5.0  # only needed to download the precomputed caches
